pipeline {
    agent any

    tools{
        maven 'Maven'
    }

    environment{
        DOCKER_IMAGE ='gestion-pointage:1.1'
    }

    stages {
       stage('Checkout') { 
            steps {
                checkout scm
            }
        }

        stage('Debug') {
            steps {
                 'pwd'
                 'ls -la'
            }
        }

        stage('Build') {
            steps {
                 './mvnw clean package'
            }
        }

        stage('Test') { 
            steps {
                 './mvnw test'
            }
        }

        stage('Build Docker Image') { 
            steps {
                 'docker build -t $DOCKER_IMAGE .'
            }
        }
        
        stage('Deploy') { 
            steps {
                 'docker run -d -p 8087:8080 --name tpdevops $DOCKER_IMAGE'
            }
        }
    }
}